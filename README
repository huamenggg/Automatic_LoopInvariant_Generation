## MIG Tutorial

#### environment
1. KLEE (need update klee include dir path in config.json)
2. Z3 (need update z3 build dir path in config.json)
3. LIBSVM (download and compile successfully, then move to the top dir of MIG, for example, InvariantGeneration/libsvm-3.24/)

#### Run command
./run.sh test_file

for example:
./run.sh Benchmark/01
